#include <bits/stdc++.h>
using namespace std;

void explainMultimap() {
    // Key-Value pairs
    // Allows duplicate keys
    multimap<int, int> mmp;
}

int main() {
    explainMultimap();
    return 0;
}
